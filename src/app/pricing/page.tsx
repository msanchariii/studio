import Pricing from "@/components/sections/Pricing";
import React from "react";

export const metadata = {
    title: "Pricing - Steadfast Studio",
    description:
        "Transparent pricing for websites, e-commerce stores, and custom solutions. No hidden fees. Get exactly what you need at a fair price.",
};

const PricingPage = () => {
    return <Pricing />;
};

export default PricingPage;
